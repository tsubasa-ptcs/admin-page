import * as React from "react";
import { Card, CardContent, CardHeader } from '@material-ui/core';

export default () => (
    <Card>
        <CardHeader title="Home" />
        <CardContent>ホーム画面です</CardContent>
    </Card>
);